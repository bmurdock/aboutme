import React from 'react';
import Navigation from './Navigation';

function Header(props) {
    return (
        <header className="mainHeader">
            <h1>Brian's Example About Me Page</h1>
            <Navigation />
        </header>
    )
}

export default Header;